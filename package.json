{
  "name": "@react-expose/monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "React Exposé - macOS Mission Control and Exposé-like experiences for web applications",
  "scripts": {
    "dev": "turbo run dev",
    "dev:lib": "turbo run dev --filter=react-expose",
    "dev:playground": "turbo run dev --filter=@react-expose/playground",
    "build": "turbo run build",
    "build:lib": "turbo run build --filter=react-expose",
    "build:playground": "turbo run build --filter=@react-expose/playground",
    "typecheck": "turbo run typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "test": "turbo run test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@changesets/cli": "^2.27.1",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "turbo": "^2.5.6"
  },
  "packageManager": "pnpm@8.14.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  }
}
